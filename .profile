# This gives us a quick way of moving directly to the Windows root
alias mongostart='sudo service mongodb start'
alias mongostop='sudo service mongodb stop'
alias mongostatus='sudo /etc/init.d/mongodb status'

